Update the backend
- Redo types, make them interfaces and declare types in the places they're used
- Add comments
- Remove methods that aren't used/can be simplified
- create seperate handlers for each game mode, add to new folder
- rename daily_reset_helpers.go to a more clear name
- Add drop method in daily_reset.go that drops all the data from the user guess collection
