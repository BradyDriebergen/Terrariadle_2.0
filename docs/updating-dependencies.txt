Go dependencies:

# See what's outdated
go list -u -m all

# Update everything to latest minor/patch versions (safest)
go get -u ./...
go mod tidy

NPM dependencies:

# See what's outdated
npm outdated

# Let npm auto-fix what it can within semver ranges
npm audit fix